# ⚡ 快速开始 - 三分钟发布应用

## 步骤 1: 本地测试（可选）

```bash
npm install
npm run tauri build
```

## 步骤 2: 创建发布标签

```bash
# 推送你的更改
git add .
git commit -m "feat: 你的改动说明"

# 创建版本标签
git tag -a v0.2.0 -m "Version 0.2.0"

# 推送标签到 GitHub
git push origin v0.2.0
```

## 步骤 3: 等待自动构建

- 打开 GitHub → Actions 标签页
- 看到三个构建任务开始运行
- 等待 10-15 分钟完成

## 步骤 4: 下载产物

### 选项 A：从 Releases 页面
- 进入 Releases 标签页
- 找到对应版本
- 下载安装包：
  - 🍎 **macOS**: `Git管理器_*.dmg` （Intel）和 `aarch64` 版本
  - 🪟 **Windows**: `Git管理器_*.msi`
  - 🐧 **Linux**: `git-manager_*.AppImage` 或 `.deb`

### 选项 B：从 Actions 页面
- 点击完成的工作流
- 向下滚动到 "Artifacts"
- 下载对应平台的文件

---

## 📋 版本标签示例

```bash
git tag -a v0.1.0 -m "Initial release"      # 初始版本
git tag -a v0.2.0 -m "Add new features"    # 小功能更新
git tag -a v0.2.1 -m "Bug fixes"            # 修复 bug
git tag -a v1.0.0 -m "Stable release"       # 正式版本
```

---

## 🆘 常见问题

### 构建失败？
检查 Actions 日志，常见原因：
- ❌ 没有推送标签（必须用 `git push origin v*`）
- ❌ 图标文件缺失（需要 `src-tauri/icons/`）
- ❌ 代码有编译错误

### 找不到下载链接？
- Releases 页面在：`github.com/你的用户名/git-manager/releases`
- 或点击仓库右侧的 "Releases" 按钮

### 如何手动触发构建？
- 不创建标签，直接在 Actions 页面点击 "Run workflow"

---

## 🎯 核心流程图

```
本地改动
    ↓
git commit
    ↓
git tag -a v0.2.0 -m "..."
    ↓
git push origin v0.2.0  ← 触发 CI/CD
    ↓
GitHub Actions 启动 (10-15 分钟)
    ├─ macOS 构建 (aarch64 + x86_64)
    ├─ Windows 构建
    └─ Linux 构建
    ↓
自动创建 Release + 上传产物
    ↓
用户下载安装 ✅
```

---

## 📦 输出产物清单

| 平台 | 文件名 | 说明 |
|------|--------|------|
| macOS | `*.dmg` | 安装程序（推荐用户使用） |
| macOS | `*.app` | 应用包 |
| Windows | `*.msi` | Windows 安装程序（推荐） |
| Windows | `*.exe` | 便携式可执行文件 |
| Linux | `*.AppImage` | 通用 Linux 应用 |
| Linux | `*.deb` | Debian 包管理器 |

---

## 💡 Pro Tips

1. **批量发布**：
   ```bash
   git tag -a v0.2.0 -m "Release" && git push origin v0.2.0
   ```

2. **自动版本号**：在 `src-tauri/tauri.conf.json` 更新版本号

3. **草稿发布**：编辑 `.github/workflows/build.yml`，改 `releaseDraft: true`

4. **预发布版本**：标签使用 `-beta`, `-rc` 后缀：
   ```bash
   git tag -a v0.2.0-beta.1 -m "Beta"
   ```

---

## 需要帮助？

查看完整指南：`.github/RELEASE_GUIDE.md`
